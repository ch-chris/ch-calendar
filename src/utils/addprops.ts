// /* eslint-disable no-console */
// import { getPublishDate } from '@finsweet/ts-utils';

// /**
//  * Greets the user by printing a message in the console.
//  * @param name The user's name.
//  */
// export const greetUser = (name: string) => {
//   const publishDate = getPublishDate();

//   console.log(`Hello ${name}!`);
//   console.log(
//     `This site was last published on ${publishDate?.toLocaleDateString('en-US', {
//       year: 'numeric',
//       month: 'long',
//       day: '2-digit',
//     })}.`
//   );
// };

// export function addProps() {
//   const eventTitle = document.querySelectorAll('.fc-event-title,.fc-list-event-title');
//   eventTitle.forEach((title) => {
//     const dept = document.createElement('p');
//     dept.classList.add('ev_department-name');
//     dept.textContent = extendedProps.department;
//     title.prepend(dept);
//   });
// }
